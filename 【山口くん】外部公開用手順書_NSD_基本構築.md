# Nsd,Nginxを用いたWeb/Dnsサーバーの構築

------------------------------

## 1. ドキュメント情報

| 項目 | 内容 |
|------|------|
| 手順書名 | 手順書_NSD_基本構築 |
| 作成日 | 2026-05-29 |
| 最終更新日 | 2026-05-29 |
| 作成者 |  |
| バージョン | v1.0 |
| 対象環境 | AWS |

> **改訂履歴**
>
> | バージョン | 日付 | 変更内容 | 変更者 |
> |-----------|------|---------|--------|
> | v1.0 | 2026-05-29 | 初版作成 |  |

------------------------------

## 2. 目的・概要

### 2-1. 目的

> 本手順書では，Webページを表示するWebサーバーと名前解決を行うDNSサーバーの構築手順について説明する．
> 構築後はブラウザで「`http://<ドメイン名>`」でアクセスした時に，Webページを閲覧可能な状態を目指す．

### 2-2. 構成概要（アーキテクチャ）


### 2-3. 完成イメージ（ゴール定義）

- [ ] ブラウザで「`http://<ドメイン名>`」でアクセスした時に，Webページを閲覧できる

------------------------------

## 3. 前提条件・準備

### 3-1. 環境要件

| 項目 | 要件 |
|------|------|
| OS | AWS（Amazon Linux 2023）,WSL（Ubuntu 24.04） |
| DNSサーバー | Nsd |
| Webサーバー | Nginx |

### 3-2. セキュリティグループ設定

| タイプ | プロトコル | ポート範囲 | ソース | 説明 |
|-------|------------|----------|--------|------|
| SSH | TCP | 22 | マイIP | ローカルPCからSSHで接続 |
| HTTP | TCP | 80 | マイIP | ローカルPCのブラウザからアクセスするため |
| DNS | UDP | 53 | 0.0.0.0/0 | どこから名前解決依頼がくるか不明なため |

------------------------------

## 4. 構築手順（詳細）

> **注意事項**
> - コマンド中の `<山カッコ>` は自分の環境の値に置き換えること

------------------------------

### Step 1 システム設定

**目的：** システムの変更と更新を行う．

#### 操作手順

```bash
# ローカルPCからsshログイン
ssh -i <秘密鍵のファイルパス> ec2-user@<EC2のパブリックIP>

# rootにユーザーにスイッチ
sudo su -

# 最新パッケージの確認
dnf update -y

# 最新パッケージの更新
dnf upgrade -y

# システムの時間を日本時間に設定(これによりログを確認した時の時間が日本時間で表示されるようになる)
timedatectl set-timezone Asia/Tokyo
```

------------------------------

### Step 1 DNSサーバーの設定

**目的：** Nsdをインストールし，DNSサーバーの設定を行う

#### 操作手順

```bash
# Amazon Linux 2023の拡張リポジトリ「SPAL」をインストール
dnf install -y spal-release

# Nsdのインストール
dnf install -y nsd

# Nsdの設定ファイルのバックアップ取得（原本保存）
cp /etc/nsd/nsd.conf{,.org}

# Nsdの設定ファイルの変更と追記
vi /etc/nsd/nsd.conf

	#---[server]セクション内に以下を追記/編集---
	ip-address: 0.0.0.0
	port: 53
	username: nsd
	zonesdir: "/etc/nsd"
	logfile: "/var/log/nsd.log"
	pidfile: "/run/nsd/nsd.pid"
	#-----------------------------------------
	#---以下を設定ファイルの一番下に追記---------
	zone:
		name: "<任意の名前>"
		zonefile "<任意の名前>.zone"
	#-----------------------------------------

# 設定ファイルの構文チェック
nsd-checkconf /etc/nsd/nsd.conf

# ゾーンデータベースファイルの作成と記入
vi /etc/nsd/<任意の名前>.zone

	#---以下を記入-----------------------------------
	$TTL 3600
	@ IN SOA ns.<任意の名前>. test.gmail.com. (
		20260501 ; serial
		3600 ; refresh
		3600 ; retry
		3600 ; expire
		3600 ) ; minimum

		IN NS ns.<任意の名前>.

	ns 	IN A <自サーバーのパブリックIP>
	www IN A <自サーバーのパブリックIP>
	#-----------------------------------------------

# ゾーンデータベースファイルの構文チェック
nsd-checkzone <任意の名前> /etc/nsd/<任意の名前>.zone

# Nsdの起動と自動起動設定
systemctl enable --now nsd

# Nsdの起動確認
systemctl status nsd | less

# Nsdの自動起動設定確認
systemctl is-enabled nsd
```

------------------------------

### Step 4 Nginxの設定

**目的：** Nginx のシステム設定と転送設定の手順について説明する．

#### 操作手順

```bash
# Nginxのインストール
dnf install -y nginx

# Nginxの起動と自動起動設定
systemctl enable --now nginx

# Nginxの起動確認
systemctl status nginx | less

# Nginxの自動起動設定確認
systemctl is-enabled nginx
```

> **確認：** Nginxの起動と設定の確認
> 
> ブラウザで「`http://<EC2のパブリックIP>`」にアクセスし，「*Welcome to Nginx!*」と表示されれば成功

> **確認：** Nsdの設定確認とNginxとの連携確認
> 
> ブラウザで「`http://<ドメイン名>`」にアクセスし，「*Welcome to Nginx!*」と表示されれば成功

------------------------------

## 付録（任意）

### A. コマンド解説

------------------------------

### B. 設定ファイル解説

------------------------------

### C. 用語解説

------------------------------

### D. 補足解説