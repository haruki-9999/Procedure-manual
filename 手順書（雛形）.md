# 作業手順書

## 1. 基本情報

| 項目    | 内容                |
| ----- | ----------------- |
| 作業名   | `<作業名>`           |
| 作業日   | `YYYY/MM/DD`      |
| 作業者   | `<作業者名>`          |
| 対象サーバ | `<ホスト名 / IPアドレス>` |
| 対象環境  | `本番 / 検証 / 開発`    |
| 作業目的  | `<何のために作業するか>`    |
| 影響範囲  | `<停止有無・影響サービス>`   |

---

## 2. 作業前確認

作業前に以下を確認する。

* [ ] 対象サーバにログインできる
* [ ] 作業対象のサーバで間違いない
* [ ] sudo権限がある
* [ ] バックアップを取得する
* [ ] 切り戻し方法を確認している

確認コマンド例：

```bash
hostname
ip addr
df -h
systemctl status <サービス名>
```

---

## 3. バックアップ

設定ファイルを変更する前にバックアップを取得する。

```bash
sudo cp -p <設定ファイル> <設定ファイル>.$(date +%Y%m%d_%H%M%S).bak
```

例：

```bash
sudo cp -p /etc/nginx/nginx.conf /etc/nginx/nginx.conf.$(date +%Y%m%d_%H%M%S).bak
```

バックアップ確認：

```bash
ls -l <設定ファイル>*
```

---

## 4. 作業手順

### 手順1：現在の状態を確認する

```bash
systemctl status <サービス名>
```

確認内容：

* サービスが起動しているか
* エラーが出ていないか

---

### 手順2：設定を変更する

```bash
sudo vi <設定ファイル>
```

変更内容：

```text
<変更内容をここに記載>
```

注意点：

* 変更前にバックアップを取る
* 変更した内容をメモする
* 不要な設定を消す場合は慎重に行う

---

### 手順3：設定に問題がないか確認する

Nginxの場合：

```bash
sudo nginx -t
```

正常例：

```text
syntax is ok
test is successful
```

その他サービスの場合：

```bash
<設定確認コマンド>
```

---

### 手順4：設定を反映する

```bash
sudo systemctl reload <サービス名>
```

reloadできない場合：

```bash
sudo systemctl restart <サービス名>
```

> 注意：`restart` はサービスが一度停止するため、本番環境では影響に注意する。

---

## 5. 作業後確認

作業後に以下を確認する。

* [ ] サービスが正常に動いている
* [ ] エラーが出ていない
* [ ] アプリにアクセスできる
* [ ] 設定変更が反映されている

確認コマンド例：

```bash
systemctl status <サービス名>
journalctl -u <サービス名> -n 50
tail -n 100 <ログファイル>
curl -I http://localhost
```

---

## 6. 切り戻し手順

問題が発生した場合は、バックアップから元に戻す。

```bash
sudo cp -p <バックアップファイル> <元の設定ファイル>
```

例：

```bash
sudo cp -p /etc/nginx/nginx.conf.20260604_100000.bak /etc/nginx/nginx.conf
```

設定確認：

```bash
sudo nginx -t
```

反映：

```bash
sudo systemctl reload <サービス名>
```

切り戻し後確認：

```bash
systemctl status <サービス名>
```

---

## 7. ログ確認

| 対象          | コマンド                                    |
| ----------- | --------------------------------------- |
| サービスログ      | `journalctl -u <サービス名> -n 50`           |
| リアルタイムログ    | `journalctl -u <サービス名> -f`              |
| Nginxエラーログ  | `tail -n 100 /var/log/nginx/error.log`  |
| Nginxアクセスログ | `tail -n 100 /var/log/nginx/access.log` |

---

## 8. 完了条件

以下が確認できれば作業完了。

* [ ] サービスが正常に起動している
* [ ] エラーが出ていない
* [ ] 疎通確認が成功している
* [ ] 作業内容を記録した
* [ ] 関係者へ完了報告した

---

## 9. 作業メモ

| 時刻      | 内容       | 結果        |
| ------- | -------- | --------- |
| `HH:MM` | `<実施内容>` | `OK / NG` |
| `HH:MM` | `<実施内容>` | `OK / NG` |

---

## 10. 報告テンプレート

### 作業完了報告

```text
関係者各位

以下の作業が完了しました。

作業名：
対象：
作業日時：
作業者：
作業結果：正常完了

確認結果：
・サービス状態：正常
・疎通確認：正常
・エラーログ：問題なし

以上です。
```

### 障害発生報告

```text
関係者各位

作業中に問題が発生しました。

作業名：
対象：
発生時刻：
事象：
影響範囲：
現在の対応：
今後の対応：

以上です。
```
