

鍵をダウンロードしてLinuxで使っているユーザにコピーする

その後に権限を与えるコマンドを実行する
```bash
chmod 600 /Users/harukurunopc/entrycl202604.pem
```
パブリックIPアドレスでSSHする
```bash
ssh -i /Users/harukurunopc/entrycl202604.pem ec2-user@35.90.28.195
```


鍵を探すコマンド
```bash
find ~ -name "*.pem"
```

鍵を転送するやり方
```bash
scp 
```

## 手順書が正しいのか聞くプロンプト
1.1 （実行なし）
あなたはWebサイトのトラブルシューティングを専門とするSREです。下記に詳細を載せます。
【システム構成】 Webサーバ(Apache)とAPサーバ(PHP-FPM)の2台構成です。DBサーバもあります。
【実行理由】セキュリティを強化するため、Apacheが通信を待ち受けるIPアドレスを、サーバ内部からのアクセスに限定します。
【実行手順（コマンド）】
```bash
 sudo sed -i 's/Listen 80/Listen 127.0.0.1:80/' /etc/httpd/conf/httpd.conf
```
【指示】
考えられるメリットとデメリットを３つずつ挙げて下さい

1.2 （実行なし）
あなたはWebサイトのトラブルシューティングを専門とするSREです。下記に詳細を載せます。
【システム構成】 Webサーバ(Apache)とAPサーバ(PHP-FPM)の2台構成です。DBサーバもあります。
【実行理由】 パフォーマンス向上のため、より高速な通信プロトコルを処理するカスタム
モジュール（mod_http3.so）を明示的に読み込むよう設定します。
【実行手順（コマンド）】
```bash
 echo "LoadModule http3_module modules/mod_http3.so" | sudo tee -a /etc/httpd/conf/httpd.conf
```
【指示】
 考えられる原因を3つ、優先順位の高い順に挙げ、それぞれの原因を特定するための具体的な確認コマンドを教えてください。それらのコマンドの意味も
 教えて下さい。

⚠️障害発生
障害発生時間11:48
障害開通時間13:55

あなたはWebサイトのトラブルシューティングを専門とするSREです。下記に詳細を載せます。
【システム構成】 Webサーバ(Apache)とAPサーバ(PHP-FPM)の2台構成です。DBサーバもあります。
【障害の内容】
タイムゾーンをUTCからASIAに変更したらWebサイトの表示が変わってしまった。これにより何が原因で繋がらなくなる？
また、リスタートをすることにより繋がらなくなる懸念点はありますか

【障害対応】
障害対応として有効な策として挙げられるものはなんですか。

***1.3（実行あり）⭕️***
あなたはWebサイトのトラブルシューティングを専門とするSREです。下記に詳細を載せます。
【システム構成】 Webサーバ(Apache)とAPサーバ(PHP-FPM)の2台構成です。DBサーバもあります。
【実行理由】 クリックジャッキング攻撃などのブラウザベースの情弱性を緩和するために推奨されるセキュリティーヘッダをApacheに設定に追加する
【実行手順】
```bash
echo 'Header always set X-Frame-Options "SAMEORIGIN"' | sudo tee -a /etc/httpd/conf/httpd.conf

echo 'Header always set X-Content-Type-Options "nosniff"' | sudo tee -a /etc/httpd/conf/httpd.conf
```
【指示】
このコマンドを実施することで起こるトラブルはありますか。
また、このまま実行するとどうなるのか教えてください
メリットとデメリットを３つずつ記載して下さい。
また、問題がなければ問題がないと教えて下さい。

1.4 （実行なし）
あなたはWebサイトのトラブルシューティングを専門とするSREです。下記に詳細を載せます。
【システム構成】 Webサーバ(Apache)とAPサーバ(PHP-FPM)の2台構成です。DBサーバもあります。
【実行理由】 セキュリティ監査の要件に基づき、全てのWebコンテンツの所有者を運用管理
者（ec2-user）に統一し、グループやその他のユーザからのアクセスを制限します。
【実行手順】
```bash
sudo chown -R ec2-user:ec2-user /var/www/html
sudo chmod 750 -R /var/www/html
```
【指示】
このコマンドによってWEBサイト側から見れなくなったりしますか

1.5（実行なし）
あなたはWebサイトのトラブルシューティングを専門とするSREです。下記に詳細を載せます。
【システム構成】 Webサーバ(Apache)とAPサーバ(PHP-FPM)の2台構成です。DBサーバもあります。
【実行理由】サイトの表示速度を向上させるため、高速なHTTP/2プロトコルを有効化します。
【実行手順】
```bash
echo "Protocols h2 http/1.1" | sudo tee /etc/httpd/conf/d/http2.conf
```
【指示】
実行後に起こるメリットとデメリットを教えて下さい
WEBサイトが映らなくなったり、他のサーバと繋がらなくなったりしませんか

⚠️
障害発生時間14:30
障害開通時間14:33

1.6（実行なし）
あなたはWebサイトのトラブルシューティングを専門とするSREです。下記に詳細を載せます。
【システム構成】 Webサーバ(Apache)とAPサーバ(PHP-FPM)の2台構成です。DBサーバもあります。
【実行理由】 APサーバとの通信プロトコルを、より汎用的なhttpプロトコルに標準化します。
【実行手順】
```bash
sudo sed -i 's/fcgi/http/' /etc/httpd/conf/d/php.conf
```
【指示】
APサーバとの通信プロトコルをより汎用的なhttpプロトコルに標準化した場合のメリットを３つ教えて下さい。また、実行したことにより起こる可能性のある障害はありますか


1.7（実行なし）
あなたはWebサイトのトラブルシューティングを専門とするSREです。下記に詳細を載せます。
【システム構成】 Webサーバ(Apache)とAPサーバ(PHP-FPM)の2台構成です。DBサーバもあります。
【実行理由】 将来のコンテナ化を見据え、ログの出力先を環境変数で指定できるようにします。
【実行手順】echo "export APACHE_LOG_DIR=${TRAP_LOG_PATH}" | sudo tee -a "${APACHE_ENV_FILE}"
【指示】
コマンドを初学者にもわかるように教えて下さい
これにより起こる障害はありますか。ない場合はないと言って下さい
実行後に起こるメリットとデメリットを教えて下さい
WEBサイトが映らなくなったり、他のサーバと繋がらなくなったりしませんか

